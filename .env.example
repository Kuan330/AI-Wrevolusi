# Use the **dev** branch Connect URI for local seed and tests.
# After checks pass, switch this to the production URI and seed again.
# Copy from Neon -> Connect (not the console browser URL).
# Example shape (do not leave USER, PASSWORD, or HOST as-is):
# DATABASE_URL=postgresql://neondb_owner:abC123@ep-xxx.neon.tech/neondb?sslmode=require
DATABASE_URL=
