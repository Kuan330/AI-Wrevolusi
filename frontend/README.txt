Frontend placeholder for Iteration 1.

The UI is not built yet. E1 should call the backend:

  GET /occupations
  GET /occupations?parent=5
  GET /occupations?parent=52
  GET /occupations?parent=522
  GET /occupations/5222/tasks

API lives in ../backend. Lookup data lives in Neon, not in this folder.
